<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Enable 2FA</title>

    <style>
        body {
            background-color: #f7f7f7;
            font-family: 'Inter', sans-serif;
        }
        .box {
            max-width: 500px;
            margin: 70px auto;
            background: white;
            padding: 40px;
            border: 1px solid #ddd;
            border-radius: 8px;
        }
        .qr-img {
            width: 200px;
            height: 200px;
        }
        .btn-custom-olive {
            background-color: #8fa83e;
            border-color: #8fa83e;
            color: white;
            font-weight: bold;
        }
        .btn-custom-olive:hover {
            background-color: #799131;
            border-color: #799131;
        }
    </style>
</head>

<body>
    <div class="box">
        <h3 class="text-center mb-4">Set Up Two-Factor Authentication</h3>

        <p class="text-secondary mb-3 text-center">
            Scan this QR code using Google Authenticator or Authy.
        </p>

        <div class="text-center mb-4">
            <img src="<%= qrData %>" class="qr-img" alt="QR Code">
        </div>

        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/setup-2fa" method="POST">
            <div class="form-group mb-3">
                <label>Enter the 6-digit code</label>
                <input type="text" name="token" maxlength="6" class="form-control" placeholder="123456" required>
            </div>

            <button class="btn btn-custom-olive w-100">Enable 2FA</button>
        </form>
    </div>
</body>
</html>