<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    
    <title>Supermarket App - Register</title>
    
    <style>
        /* 1. Global Background Color */
        body {
            background-color: #f7f7f7; /* Very light gray */
            font-family: 'Inter', sans-serif; /* Applied Inter font globally */
        }

        /* Added for better hierarchy */
        h1, h2, h3, h4, h5, h6 {
            font-weight: 600; 
        }

        /* 2. Custom Header Styles (The olive-green bar) */
        .custom-header-bg {
            background-color: #8fa83e; /* The dark olive green */
            padding: 10px 0;
        }
        .header-content {
            display: flex;
            justify-content: space-between; /* Ensures the logo and link are pushed to opposite ends */
            align-items: center;
            max-width: 1200px; /* Max width to center the content */
            margin: 0 auto;
            padding: 0 15px;
        }
        .app-logo {
            font-size: 24px;
            font-weight: bold;
            color: #ffffff;
            text-decoration: none;
        }
        
        .contact-link {
            color: #ffffff; 
            text-decoration: none; 
            font-size: 18px;
            padding: 5px 10px;
            border: 1px solid transparent; 
            border-radius: 4px;
            transition: background-color 0.3s;
        }
        
        .contact-link:hover {
            background-color: rgba(255, 255, 255, 0.1); 
        }

        /* 3. Main Container for Register/Login Form */
        .register-box-container {
            /* Wider container needed to fit more form fields cleanly */
            max-width: 900px; 
            margin: 40px auto; 
            background-color: #ffffff;
            border: 1px solid #ddd;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
        }

        /* 4. Custom Button Styles (Olive Green) */
        .btn-custom-olive {
            background-color: #8fa83e;
            border-color: #8fa83e;
            color: white;
            padding: 10px 25px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 4px;
        }
        .btn-custom-olive:hover {
            background-color: #799131;
            border-color: #799131;
            color: white;
        }
        
        /* 5. Form Group Styling for consistency */
        .form-control {
            border-radius: 4px;
        }

        /* Hiding original Bootstrap navbar */
        .navbar { display: none; }
    </style>
</head>

<body class="d-flex flex-column min-vh-100">
    <div class="custom-header-bg">
        <div class="header-content">
            <a class="app-logo" href="/">
                OneStopMart
            </a>
            <a href="/contact" class="contact-link">
                Need help? Contact us!
            </a>
        </div>
    </div>
    
    <div class="flex-grow-1">
        <div class="container register-box-container">
            <div class="row">
                <div class="col-md-7 p-5 border-end"> 
                    <h3 class="mb-4">Register for an Account</h3>
                    
                    <% if (messages && messages.length > 0) { %>
                        <div class="alert alert-danger p-2 small">
                            <% messages.forEach(function(message) { %>
                                <p class="m-0"><%= message %></p>
                            <% }); %>
                        </div>
                    <% } %>
                    
                    <form action="/register" method="POST" class="mt-4">
                        <div class="row">
                            <div class="col-md-6 form-group mb-3">
                                <label for="username">Username:</label>
                                <input type="text" id="username" name="username" class="form-control" required 
                                <% if (formData && formData.username) { %> value="<%= formData.username %>" <% } else { %> value="" <% } %>>
                            </div>
                            <div class="col-md-6 form-group mb-3">
                                <label for="email">Email:</label>
                                <input type="email" id="email" name="email" class="form-control" required 
                                <% if (formData && formData.email) { %> value="<%= formData.email %>" <% } else { %> value="" <% } %>>
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            <label for="password">Password:</label>
                            <input type="password" id="password" name="password" class="form-control" required>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label for="address">Address:</label>
                            <input type="text" id="address" name="address" class="form-control" required 
                            <% if (formData && formData.address) { %> value="<%= formData.address %>" <% } else { %> value="" <% } %>>
                        </div>
                        
                        <div class="form-group mb-4">
                            <label for="contact">Contact Number:</label>
                            <input type="text" id="contact" name="contact" class="form-control" required 
                            <% if (formData && formData.contact) { %> value="<%= formData.contact %>" <% } else { %> value="" <% } %>>
                        </div>
                        
                        <button type="submit" class="btn btn-custom-olive w-100">Register</button>
                    </form>
                </div>

                <div class="col-md-5 p-5 d-flex flex-column justify-content-center bg-light">
                    <h3 class="mb-3">Already have an account?</h3>
                    <p class="text-secondary mb-4">
                        Welcome back! Log in to continue your seamless online shopping experience.
                    </p>
                    <a href="/Login" class="btn btn-custom-olive" style="width: 150px;">Log in</a>
                </div>
            </div>
        </div>
    </div>

    <%- include("partials/footer") %>
</body>
</html>