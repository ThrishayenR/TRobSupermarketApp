<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Two-Factor Authentication</title>
    <style>
        body {
            background-color: #f7f7f7;
            font-family: 'Inter', sans-serif;
        }
        .box {
            max-width: 500px;
            margin: 70px auto;
            background: white;
            padding: 40px;
            border: 1px solid #ddd;
            border-radius: 8px;
        }
        .btn-custom-olive {
            background-color: #8fa83e;
            border-color: #8fa83e;
            color: white;
            font-weight: bold;
        }
        .btn-custom-olive:hover {
            background-color: #799131;
            border-color: #799131;
        }
    </style>
</head>

<body>
    <div class="box">
        <h3 class="text-center mb-4">Enter Your 2FA Code</h3>

        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/verify-2fa" method="POST">
            <div class="form-group mb-3">
                <label>6-digit Code</label>
                <input type="text" name="token" maxlength="6" class="form-control" placeholder="123456" required>
            </div>

            <button class="btn btn-custom-olive w-100">Verify</button>
        </form>
    </div>
</body>
</html>